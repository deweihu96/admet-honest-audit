# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Cached TDC benchmark CSVs (data/admet_group/) ARE committed so a fresh clone
# reproduces with no download. Snapshot is PyTDC 1.1.15; TDC has no dataset
# versioning, so this pins reproducibility to a fixed snapshot.
2026.02.26.708193v1.full.pdf
stage6.out
stage6.sbatch
stage6_cleanup.py

# macOS
.DS_Store
